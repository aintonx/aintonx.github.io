.DS_Store
__MACOSX/
*.zip
*.tar
*.tar.gz
*.tgz
0. NOT GitHub/
BACKUP/
BACKUP 2/
work/
outputs/

# инструменты качества (в деплой не идут, pages.yml — белый список)
node_modules/
tools/shots/

# ── ВЫНЕСЕНО ИЗ ПУБЛИЧНОГО РЕПОЗИТОРИЯ 10.08.2026 ──
# Репозиторий публичный: всё, что здесь лежит, видно любому человеку.
# Перечисленное ниже переехало в ~/Downloads/секреты — там бизнес-модель,
# юридическая стратегия, персональные данные, устройство базы и бэкенда,
# невыпущенные макеты. Сайту ничего из этого не нужно: pages.yml копирует
# только index.html, CNAME, robots.txt, sitemap.xml, файлы подтверждения
# и папки logo, share, img, Character.
# Если вернёте что-то из этого в папку — оно НЕ уедет на GitHub, пока
# строка ниже на месте. Убирать строки осознанно.
docs/
design/
data/
scripts/
api/
work-rec.mp4
